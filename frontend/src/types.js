export const Priority = {
    LOW: 'low',
    MEDIUM: 'medium',
    HIGH: 'high',
    URGENT: 'urgent'
};

// Task interface for reference
/*
export interface Task {
  id: string;
  title: string;
  notes?: string;
  dueDate?: number;
  priority: string;
  isCompleted: boolean;
  tags: string[];
  subtasks: Subtask[];
  createdAt: number;
}

export interface Subtask {
  id: string;
  title: string;
  isCompleted: boolean;
}
*/
